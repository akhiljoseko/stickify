/// Routing layer — barrel export.
///
/// Provides access to the router factory and all route data classes.
/// The generated `router.g.dart` is not exported directly — it is pulled in
/// automatically via the `part` directive inside `router.dart`.
library;

export 'app_shell.dart';
export 'router.dart';
